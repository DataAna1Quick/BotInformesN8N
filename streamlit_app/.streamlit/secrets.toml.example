# Plantilla — copiar a `secrets.toml` y rellenar (NUNCA commitear secrets.toml)

ANTHROPIC_API_KEY = "sk-ant-api03-REEMPLAZAR"

# Hash bcrypt del password "QuickHelp2026"
# Generar con:
#   python -c "import bcrypt; print(bcrypt.hashpw(b'QuickHelp2026', bcrypt.gensalt(12)).decode())"
APP_PASSWORD_HASH = "$2b$12$REEMPLAZAR"

DEFAULT_MODEL = "claude-haiku-4-5-20251001"
PREMIUM_MODEL = "claude-sonnet-4-6"
ENABLE_LLM = true
